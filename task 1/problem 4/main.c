#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x=7 ,y=4;
    printf("(x | y)=%d \n (x & y)=%d \n (x ^ y)=%d \n (x <<1)=%d \n (x >> 2)=%d",(x | y) ,(x & y),(x ^ y),(x << 1),(x >> 2));
    return 0;
}
